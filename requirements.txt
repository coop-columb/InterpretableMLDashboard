# requirements.txt

fastapi
uvicorn[standard] # Includes standard dependencies like websockets
dash
dash-bootstrap-components
plotly
pandas
numpy
tensorflow # Or tensorflow-cpu if you don't need GPU support initially
# Add specific versions if needed, e.g., fastapi==0.100.0
# Required for file uploads (FastAPI)
python-multipart

# Testing
pytest
httpx

# Frontend / API Communication
requests

# Image Processing
Pillow
